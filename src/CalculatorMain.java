public class CalculatorMain {
    public static void main(String[] args) {
        Calculator calc = new Calculator(10);
        calc.plus();
        calc.minus();
        calc.multiple();
        calc.divide();
    }
}
